2026-07-03: The Codex CLI argument list is centralized in `buildCodexArgs` and reused by both `codex` and `ollama-codex` runners so sandbox, approval, stdin, and session-resume argument ordering cannot drift.
2026-07-03: The init screen (first-run setup wizard) now offers an "inherit" option for Runner, Safety, and Branches, mirroring the settings screen project-scope inherit behavior. Inherit is the default for all three fields, saving empty/nil project settings so they resolve from the global configuration at merge time. The SetupCompletedMsg handler reloads merged settings to derive NoBranchChange and Unrestricted flags, matching SettingsSavedMsg behavior.
2026-07-16: Cyclestone uses optional root `AGENTS.md` for concise current operating instructions when present and keeps `.cyclestone/DECISIONS.md` as the separate chronological decision log. Agent cycles may propose `AGENTS.md` updates in handoff data, but applying them requires an explicit human review action.
2026-07-17: Executor phases protect canonical root `AGENTS.md` from ordinary agent edits by snapshotting it around runner execution, restoring any created/modified/deleted mutation, and storing proposed replacement content in the phase handoff for explicit human review.
2026-07-17: Recommender handoffs keep `score` as the cycle-continuation recommendation and add a separate `agent_instructions_update_score` for human review of root `AGENTS.md`; state and reports persist/display the two scores independently and use `-1`/N/A when either score is absent.
2026-07-17: Untracked embedded Git repositories are surfaced as cycle report and metadata `informational_warnings` for human awareness. These warnings are excluded from recommender summaries and should not increase continuation scores unless the active milestone explicitly targets repository topology or embedded repository contents.
2026-07-17: The explicit `Update AGENTS.md` workflow reuses milestone runner selection, optional-message entry, preflight validation, and runner display, but executes a single updater runner with either repository-wide or selected-milestone context. Runner edits to root `AGENTS.md` are still intercepted and restored; proposals are saved under `.cyclestone/temp/AGENTS.md.proposed` and require explicit TUI apply action.
2026-07-17: Milestone cycle artifacts are stored under `.cyclestone/reports/<milestone-id>/`, with `summary.md` at the milestone root, `cycle-NNN/report.yaml` and `metadata.json` per cycle, and ordered per-agent phase directories containing `input.md`, `output.log`, and `handoff.yaml`. Executor prompt reads, summaries, TUI history, and deletion flows use the hierarchical paths only; AGENTS.md update reports remain in their separate reports namespace.
2026-07-20: Planning-layer persistence lives in `internal/config` as an optional API over `.cyclestone/plans/*.yml`. Planning load/save and validation are isolated from existing Milestone config, specs, state, and reports; dangling optional planning references are surfaced as planning warnings rather than Milestone storage errors.
2026-07-20: Read-only planning CLI navigation is handled in `cmd/cyclestone` before TUI startup for explicit `plan list`, `plan show <plan-id>`, and `briefing show <plan-id> <briefing-id>` commands. These commands load planning files relative to the configured milestone index and use milestone IDs only to label Briefing relationships as none, linked, or missing; they do not create, migrate, save, execute, or delete planning or milestone artifacts.
2026-07-20: Manual planning management extends the same pre-TUI `cmd/cyclestone` command path for explicit `plan` and `briefing` mutations. Mutating planning commands validate all Plan files before writes, save only typed `.cyclestone/plans/<plan-id>.yml` records through `config.SavePlan`, require exact confirmation tokens for Plan and Briefing deletion, and enforce strict Briefing-to-Milestone links against the compact Milestone index without mutating Milestone specs, index entries, state, reports, temp files, or branch snapshots.
2026-07-20: AI-assisted Plan generation is a pre-TUI `plan generate` command that captures a structured JSON proposal from a local runner or response file, converts it into an in-memory `config.Plan`, validates it through the planning validator, and then saves only `.cyclestone/plans/<plan-id>.yml` unless `--preview` is used. Generated Briefings are active, same-Plan only, and forbidden from carrying `milestone_id`, so Milestone specs, compact indexes, state, reports, temp files, cycles, and branch snapshots remain independent.
2026-07-20: Plan review uses the existing planning status model and pre-TUI mutation path: approval maps to `completed`, rejection maps to `archived`, and split/merge rewrite only the containing typed Plan file through `config.SavePlan`. Split and merge require explicit Milestone link choices when a link could otherwise be lost or chosen ambiguously, and neither command creates, edits, or deletes Milestone specs, compact indexes, state, reports, temp files, cycles, or branch snapshots.
2026-07-20: Briefing-to-Milestone generation is a pre-TUI `briefing generate-milestone` mutation that accepts only completed Briefings with completed same-Plan dependencies, writes one ordinary Markdown Milestone spec plus compact index entry, and only after successful Milestone creation saves the generated `milestone_id` on the source Briefing. Preview mode writes nothing, replacement changes only the Briefing link, and generated Milestones remain executable without planning files or source Plan availability.
2026-07-20: Briefing-to-Milestone relation review remains a pre-TUI planning concern: CLI detail views derive standalone/missing/cross-Plan relation labels from loaded Plan files plus the compact Milestone ID set. Manual link replacement is allowed only with `briefing link --replace-link`, validates the replacement Milestone exists, blocks active/completed cross-Plan duplicate links, and updates only the containing typed Plan file.
2026-07-20: `briefing execute` resolves or generates exactly one ordinary Milestone in the planning CLI layer, then queues the existing TUI preflight and `executor.ExecuteCycle` workflow with minimal immutable Plan/Briefing origin metadata kept out of `internal/executor`. Approved results best-effort complete only the selected Briefing after reloading and verifying its link; other results and cancellation do not advance planning, and Plan update failures never invalidate Milestone artifacts.
2026-07-20: Explicit `plan start` and `plan resume` orchestration is owned by the pre-TUI planning CLI and optional typed Plan `execution` checkpoints. The coordinator selects only same-Plan dependency-ready Briefings, resolves or generates one ordinary linked Milestone at a time, and carries immutable queue context through the TUI while `internal/executor` remains planning-agnostic. Resume reloads Plan, compact index, and state; deterministic generated artifacts interrupted before link persistence are reclaimed only after their ID, title, spec path, and embedded Plan/Briefing markers match. Approval and launch callbacks revalidate the live retained identity, cancellation navigation retains terminal callback ownership, and an uncertain active-cycle checkpoint remains non-launchable across repeated resumes until runtime state is terminal. Plan lifecycle operations never cascade into Milestone artifacts.
2026-07-20: The planning hierarchy tree renderer is implemented in `internal/tui/tree.go` as a shared component for CLI (`cyclestone plan tree` and `cyclestone plan tree <plan-id>`) and TUI (`DashboardModel` tree view toggle). It builds a structured node tree starting from virtual root "Milestone Planner" down through Plans, Briefings, linked Milestones, and Milestone Cycles, while strictly excluding standalone Milestones. Terminal width truncation and VS Code integrated terminal / explicit `--ascii` branch glyph fallbacks (`|--`, `\--`, `|`) are strictly enforced without mutating planning files or milestone artifacts.
2026-07-20: TUI planning navigation is structured as a multi-level flat table and detail hierarchy (ScreenPlans -> ScreenPlanDetails -> ScreenBriefingDetails). Plans are rendered in a flat table with ID, Title, Status, Briefings progress, and Execution state. Selecting a Plan opens Plan details with metadata and a flat table of Briefings with ID, Title, Status, Dependencies, and Milestone Link. Selecting a Briefing opens Briefing details styled matching Milestone details with Objective, Intent, Completion Signal, Constraints, and linked Milestone status/cycle logs. Multi-level Esc/Backspace step-back navigation is strictly enforced while preserving pre-existing CLI tree rendering.
2026-07-20: Lifecycle safety and referential integrity between Plans, Briefings, and Milestones are strictly enforced across CLI, TUI, and AI planning workflows. Archiving, deleting, or unlinking Plans or Briefings mutates only planning YAML files under .cyclestone/plans/ and never deletes or alters linked Milestone specs, index entries, runtime state, reports, or branch snapshots. Deleting a Milestone leaves referring Briefings in a safe missing <id> state without corrupting Plan validation or blocking unrelated Briefing execution. Standalone and generated Milestones function as fully independent first-class entities regardless of source Plan or Briefing archival, deletion, or missing status.
2026-07-20: TUI Plan creation and deletion are planning-only mutations owned by RootModel. Creation collects Plan ID, title, and objective, derives stable metadata, validates all existing Plan files, persists through config.SavePlan, and synchronizes Dashboard and Plans only after a successful reload. Deletion requires the exact Plan ID, uses config.DeletePlan to remove one typed YAML record, preserves all Milestone artifacts, and clears stale non-running TUI Plan references after reload; persistence and reload failures retain their focused form or confirmation context.
2026-07-20: CreatePlanModel in internal/tui/plan_management.go is updated to match CreateMilestoneModel style, design, and workflow. Form rendering and height recalculations reuse the shared FormBlock, RenderBoundedBlocks
2026-07-22: Briefing detail navigation into milestone creation uses a typed, immutable Plan/Briefing display snapshot carried only through the TUI screen-change payload. The existing CreateMilestoneMsg, persistence handler, branch behavior, and post-creation navigation remain unchanged; briefing context is optional display-only state with a bounded viewport.
2026-07-22: Superseding the earlier display-only interpretation, Briefing-derived milestone creation prefills the complete immutable Plan/Briefing snapshot into the existing editable Goal / Description textarea. The form uses its ordinary textarea scrolling, validation, submission message, persistence, branch behavior, and navigation; ordinary milestone creation and cycle-note modes remain unprefilled.
